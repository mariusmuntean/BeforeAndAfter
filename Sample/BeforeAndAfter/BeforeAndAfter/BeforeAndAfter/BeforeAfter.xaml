<?xml version="1.0" encoding="UTF-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BeforeAndAfter.BeforeAfter">
    <ContentView.Resources>
        <x:Double x:Key="Offset">50.0</x:Double>
        <x:Double x:Key="NegativeOffset">-50.0</x:Double>
    </ContentView.Resources>

    <Grid x:Name="LayoutRoot">

        <!-- beforeView is added here in the code behind -->
        <d:BoxView BackgroundColor="Gold"/>

        <Grid x:Name="AfterViewParentLayout"
              IsClippedToBounds="True"
              TranslationX="{StaticResource Offset}">

            <!-- afterView is added here in the code behind -->
            <d:BoxView BackgroundColor="YellowGreen"
                       TranslationX="{StaticResource NegativeOffset}" />
        </Grid>
    </Grid>
</ContentView>